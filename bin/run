#!/usr/bin/env node

const { config } = require('dotenv');
const { resolve } = require('path');
config({
    path: resolve(__dirname + '/../.env'), // .env must be in the root of the project.
});

const main = require('../lib/index').default;

main();
